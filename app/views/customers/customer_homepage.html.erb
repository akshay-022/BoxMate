<!--  This file is app/views/movies/index.html.erb -->
<h2>All Chefs</h2>

<%#  Part 2: Start here... %>
<%= form_tag chefs_path, method: :get, id: 'cuisine_selection_form' do %>
  <% @all_cuisines.each do |cuisine| %>
    <div class="form-check  form-check-inline">
      <%= label_tag "cuisines[#{cuisine}]", cuisine, class: 'form-check-label' %>
      <%= check_box_tag "cuisines[#{cuisine}]", "1",  1, class: 'form-check-input' %>
    </div>
  <% end %>
  <%= submit_tag 'Refresh', id: 'ratings_submit', class: 'btn btn-primary' %>
<% end %>

<table class="table table-striped col-md-12" id="chefs">
  <thead>
    <tr>
      <th>Chef's Name</th>
      <th>Description</th>
      <th>Address</th>
    </tr>
  </thead>
  <tbody>
    <% @chefs.each do |chef| %>
      <tr>
        <td>
          <%= chef.name %>
        </td>
        <td>
          <%= chef.description %>
        </td>
        <td>
          <%= chef.address %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>